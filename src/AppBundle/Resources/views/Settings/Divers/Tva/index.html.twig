{% extends "base.html.twig" %}

{% block title %}
    {{ '%entity% list'|trans({'%entity%': 'gestock.settings.diverse.vat'|trans}, 'admin') }} - {{ parent() }}
{% endblock %}

{% block body_title %}
    <span class="glyphicon glyphicon-piggy-bank"></span>
    {{ '%entity% list'|trans({'%entity%': 'gestock.settings.diverse.vat'|trans}, 'admin') }}
{% endblock %}

{% block glstock_body %}
    <table class="table table-striped table-hover table-bordered records_list">
        <thead>
            <tr>
                <th scope="col" class="col-md-10">{{ 'gestock.settings.diverse.vat'|trans }}</th>
                <th scope="col" class="col-md-2 text-center">{{ 'Actions'|trans({}, 'admin') }}</th>
            </tr>
        </thead>
        <tbody>
            {% for tva in entities %}
                <tr>
                    <td><a href="{{ path('tva_show', {id: tva.id}) }}">{{ tva.rate * 100 }} %</a></td>
                    <td class="text-center">
                        <div class="btn-group">
                            <a class="btn btn-default btn-show btn-xs" href="{{ path('tva_show', {id: tva.id}) }}">
                                {{ 'show'|trans({}, 'admin') }}
                            </a>
                            <a class="btn btn-info btn-edit btn-xs" href="{{ path('tva_edit', {id: tva.id}) }}">
                                {{ 'edit'|trans({}, 'admin') }}
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="btn-group">
        {% include 'homeButton.html.twig' %}
        <a class="btn btn-info btn-create" href="{{ path('tva_new') }}">
            {{ 'Create a new entry'|trans({}, 'admin') }}
        </a>
    </div>
    
{% endblock %}
