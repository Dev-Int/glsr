{% extends "base.html.twig" %}

{% block title %}
    {{ '%entity% list'|trans({'%entity%': 'ZoneStorage'}, 'admin') }} - {{ parent() }}
{% endblock %}

{% block body_title %}
    <span class="glyphicon glyphicon-map-marker"></span>
    {{ '%entity% list'|trans({'%entity%': 'gestock.settings.diverse.zonestorage'|trans()}, 'admin') }}
{% endblock %}

{% block glstock_body %}
    <table class="table table-striped table-hover table-bordered records_list">
        <thead>
            <tr>
                <th scope="col" class="col-md-10">{{ 'gestock.name'|trans }}</th>
                <th scope="col" class="col-md-2 text-center">{{ 'Actions'|trans({}, 'admin') }}</th>
            </tr>
        </thead>
        <tbody>
            {% for zonestorage in entities %}
                <tr>
                    <td><a href="{{ path('zonestorage_show', {slug: zonestorage.slug}) }}">{{ zonestorage.name }}</a></td>
                    <td class="text-center">
                        <div class="btn-group">
                            <a class="btn btn-default btn-show btn-xs" href="{{ path('zonestorage_show', {slug: zonestorage.slug}) }}">
                                {{ 'show'|trans({}, 'admin') }}
                            </a>
                            <a class="btn btn-info btn-edit btn-xs" href="{{ path('zonestorage_edit', {slug: zonestorage.slug}) }}">
                                {{ 'edit'|trans({}, 'admin') }}
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="btn-group">
        {% include 'homeButton.html.twig' %}
        <a class="btn btn-info btn-create" href="{{ path('zonestorage_new') }}">
            {{ 'Create a new entry'|trans({}, 'admin') }}
        </a>
    </div>
    
{% endblock %}
