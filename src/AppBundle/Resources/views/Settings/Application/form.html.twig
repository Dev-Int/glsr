{% if edit_form is defined %}
    {% set form = edit_form %}
    {{ form_start(form) }}
{% else %}
    {{ form_start(form) }}
{% endif %}
{% form_theme form 'AppBundle:Form:bootstrap_3_horizontal_layout.html.twig' %}

{{ form_start(form) }}
    {#<div class="form-group">#}{{ form_row(form.inventory_style) }}{#</div>#}
    {#<div class="form-group">#}{{ form_row(form.calculation) }}{#</div>#}
    {#<div class="form-group">#}{{ form_row(form.currency) }}{#</div>#}
    
    <div class="form-group">
        <div class="btn-group record_actions">
        {% if edit_form is defined %}
            {{ form_widget(form.save) }}
        {% else %}
            {{ form_widget(form.save) }} <div style="display:none">{{ form_widget(form.addmore) }}</div>
        {% endif %}
        </div>
    </div>
{{ form_end(form) }}
