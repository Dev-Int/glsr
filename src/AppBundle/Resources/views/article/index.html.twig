{% extends "base.html.twig" %}

{% block title %}
    {{ '%entity% list'|trans({'%entity%': 'title'|trans({}, 'gs_articles')}, 'admin') }} - {{ parent() }}
{% endblock %}
{% macro thead(name, field, label) %}
    {% spaceless %}
        {% set sort = app.session.get('sort.' ~ name) %}
        {% if sort and sort.field == field %}
            {% if sort.type == 'DESC' %}
                <a href="{{ path('article_sort', {field: field, type: 'ASC'}) }}">{{ label }} <i class="fa fa-sort-down"></i></a>
            {% elseif sort.type == 'ASC' %}
                <a href="{{ path('article_sort', {field: field, type: 'DESC'}) }}">{{ label }} <i class="fa fa-sort-up"></i></a>
            {% endif %}
        {% else %}
            <a href="{{ path('article_sort', {field: field, type: 'ASC'}) }}">{{ label }} <i class="fa fa-sort"></i></a>
        {% endif %}
    {% endspaceless %}
{% endmacro %}

{% from _self import thead %}

{% block body_title %}
    <i class="fa fa-shopping-basket" aria-hidden="true"></i> 
    {{ '%entity% list'|trans({'%entity%': 'title'|trans({}, 'gs_articles')}, 'admin') }}
{% endblock %}

{% block glstock_body %}
    <table class="table table-striped table-hover table-bordered records_list">
        <thead>
            <tr>
                <th scope="col" class="col-md-1">{{ 'gestock.id'|trans }}</th>
                <th scope="col" class="col-md-3">{{ thead('article', 'name', 'gestock.name'|trans) }}</th>
                <th scope="col" class="col-md-2">
                    <i class="fa fa-barcode"></i> 
                    {{ 'title'|trans({}, 'gs_suppliers') }}
                </th>
                <th scope="col" class="col-md-1 text-center">
                    <i class="fa fa-tags"></i> 
                    {{ 'gestock.settings.diverse.familylog'|trans }}
                </th>
                <th scope="col" class="col-md-1 text-center">{{ 'settings.price'|trans({}, 'gs_articles') }}</th>
                <th scope="col" class="col-md-1 text-center">{{ 'settings.quantity'|trans({}, 'gs_articles') }}</th>
                <th scope="col" class="col-md-1 text-center">
                    <i class="fa fa-exclamation-triangle"></i> 
                    {{ 'settings.stock_alert'|trans({}, 'gs_articles') }}
                </th>
                <th scope="col" class="col-md-2 text-center">
                    <i class="fa fa-hourglass-o"></i> 
                    {{ 'Actions'|trans({}, 'admin') }}
                </th>
            </tr>
        </thead>
        <tbody>
            {% for article in paginator %}
                <tr>
                    <td>{{ "%06d"|format(article.id) }}</td>
                    <td><a href="{{ path('article_show', {slug: article.slug}) }}">{{ article.name }}</a></td>
                    <td><a href="{{ path('supplier_show', {slug: article.supplier.slug}) }}">{{ article.supplier.name }}</a></td>
                    <td>{{ article.familylog }}</td>
                    <td class="text-right">{{ article.price }}€</td>
                    <td class="text-right">{{ article.quantity }}</td>
                    <td class="text-right">{{ article.minstock }}</td>
                    <td class="text-center">
                        <div class="btn-group">
                            <a class="btn btn-default btn-sm" href="{{ path('article_show', {slug: article.slug}) }}">
                                <i class="fa fa-file-o"></i> {{ 'show'|trans({}, 'admin') }}
                            </a>
                            {% if is_granted('ROLE_ADMIN') %}
                            <a class="btn btn-info btn-sm" href="{{ path('article_edit', {slug: article.slug}) }}">
                                <i class="fa fa-edit"></i> {{ 'edit'|trans({}, 'admin') }}
                            </a>
                            {% endif %}
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="text-center">{{ knp_pagination_render(paginator) }}</div>
    
    <div class="new_entry list-unstyled">
        {% include 'homeButton.html.twig' %}
        {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-info" href="{{ path('article_new') }}">
            <i class="fa fa-plus"></i> {{ 'Create a new entry'|trans({}, 'admin') }}
        </a>
        {% endif %}
    </div>
{% endblock %}
