{% extends "base.html.twig" %}

{% block title %}
    {{ '%entity% list'|trans({'%entity%': 'title'|trans({}, 'gs_suppliers')}, 'admin') }} - {{ parent() }}
{% endblock %}

{% macro thead(name, entity, field, label) %}
    {% spaceless %}
        {% set sort = app.session.get('sort.' ~ name) %}
        {% if sort and sort.entity == entity and sort.field == field %}
            {% if sort.type == 'DESC' %}
                <a href="{{ path('supplier_sort', {entity: entity, field: field, type: 'ASC'}) }}">{{ label }} <i class="fa fa-sort-down"></i></a>
            {% elseif sort.type == 'ASC' %}
                <a href="{{ path('supplier_sort', {entity: entity, field: field, type: 'DESC'}) }}">{{ label }} <i class="fa fa-sort-up"></i></a>
            {% endif %}
        {% else %}
            <a href="{{ path('supplier_sort', {entity: entity, field: field, type: 'ASC'}) }}">{{ label }} <i class="fa fa-sort"></i></a>
        {% endif %}
    {% endspaceless %}
{% endmacro %}

{% from _self import thead %}

{% block body_title %}
    <i class="fa fa-barcode" aria-hidden="true"></i>
    {{ '%entity% list'|trans({'%entity%': 'title'|trans({}, 'gs_suppliers')}, 'admin') }}
{% endblock %}

{% block glstock_body %}
    <table class="table table-striped table-hover table-bordered records_list">
        <thead>
            <tr>
                <th scope="col" class="col-md-1 text-center">{{ thead('supplier', 'supplier', 'id', 'gestock.id'|trans) }}</th>
                <th scope="col" class="col-md-2 text-center">{{ thead('supplier', 'supplier', 'name', 'gestock.name'|trans) }}</th>
                <th scope="col" class="col-md-3 text-center">
                    <i class="fa fa-map-marker"></i> 
                    {{ 'gestock.address'|trans }}
                </th>
                <th scope="col" class="col-md-2 text-center">
                    <i class="fa fa-phone"></i> 
                    {{ 'gestock.phone'|trans }}
                </th>
                <th scope="col" class="col-md-2 text-center">
                    <i class="fa fa-tags"></i> 
                    {{ thead('supplier', 'familyLog', 'name', 'gestock.settings.diverse.familylog'|trans) }}
                </th>
                <th scope="col" class="col-md-2 text-center">
                    <i class="fa fa-hourglass-o"></i> 
                    {{ 'Actions'|trans({}, 'admin') }}
                </th>
            </tr>
        </thead>
        <tbody>
            {% for supplier in paginator %}
                {% if supplier.active == 0 %}
                    <tr class="alert alert-danger">
                {% else %}
                    <tr>
                {% endif %}
                    <td class="text-center">{{ "%06d"|format(supplier.id) }}</td>
                    <td><a href="{{ path('supplier_show', {slug: supplier.slug}) }}">{{ supplier.name }}</a></td>
                    <td><div class="text-20-letters">{{ supplier.address }}</div><div class="text-5-letters">{{ supplier.zipcode }}</div><div class="text-15-letters">{{ supplier.town }}</div></td>
                    <td class="text-center">{{ supplier.phone|phone_number_format('NATIONAL') }}</td>
                    <td class="text-center">{{ supplier.familylog }}</td>
                    <td class="text-center">
                        <div class="btn-group">
                            <a class="btn btn-default btn-show btn-sm" href="{{ path('supplier_show', {slug: supplier.slug}) }}">
                                {{ 'show'|trans({}, 'admin') }}
                            </a>
                                {% if is_granted('ROLE_ADMIN') %}
                            <a class="btn btn-info btn-edit btn-sm" href="{{ path('supplier_edit', {slug: supplier.slug}) }}">{{ 'edit'|trans({}, 'admin') }}
                            </a>
                                {% endif %}
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    {% if is_granted('ROLE_ADMIN') %}
        <div>
            <p class="alert alert-danger">Fournisseurs supprimés.</p>
        </div>
    {% endif %}
    
    <div class="text-center">{{ knp_pagination_render(paginator) }}</div>
    
    <div class="btn-group">
        {% include 'homeButton.html.twig' %}
        {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-info btn-create" href="{{ path('supplier_new') }}">
            {{ 'Create a new entry'|trans({}, 'admin') }}
        </a>
        {% endif %}
    </div>
    
{% endblock %}
