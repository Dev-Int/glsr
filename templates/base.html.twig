<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}GLSR{% endblock %}</title>
        <link rel="icon" href="favicon.ico" />
        {% block stylesheets %}
            <link rel="stylesheet" href="https://bootswatch.com/4/lumen/bootstrap.min.css">
            <link rel="stylesheet" href="{{ asset('build/app.css') }}">
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="{{ path('home') }}"><i class='icon-GLSR'></i></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('home') }}">{{ 'menu.home'|trans }} <span class="sr-only">(current)</span></a>
                </li>
            </ul>
            <ul class="navbar-nav mr-sm-2">
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('easyadmin') }}">Admin</a>
                    </li>
                {% endif %}
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('securityLogin') }}" class="nav-link">Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('securityLogout') }}" class="nav-link">Déconnexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        </nav>

        <div class="container">
            {% block body %}
            {% endblock %}
        </div>
        {% block javascripts %}
            <script src="{{ asset('build/app.js') }}"></script>
        {% endblock %}
    </body>
</html>
